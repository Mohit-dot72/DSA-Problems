class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* prev = nullptr;
        ListNode* temp = head;
        while(temp != nullptr){
            ListNode* temp1 = temp->next;
            temp->next = prev; 
            prev = temp;
            temp = temp1;
        }
        return prev;
    }
};
